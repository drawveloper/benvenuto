(function() {
  var assert;

  assert = require('assert');

  describe('main', function() {
    return it('should be true', function() {
      var x;
      x = true;
      return assert.equal(x, true);
    });
  });

}).call(this);
