// Generated by CoffeeScript 1.3.3
(function() {
  var codename, coffee_css, coffeecup_adapter, coffeescript_helpers, copy_data_to, express, express_partials, fs, jquery, jsdom, log, minify, native_array, native_exists, native_existsSync, native_name, native_readFile, native_readFileSync, path, sammy, socketio, socketio_key, socketjs, uglify, uuid, vendor, zappa, zappa_fs, _ref, _ref1,
    __slice = [].slice,
    __indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

  zappa = {
    version: '0.4.10'
  };

  codename = 'Freak Out!';

  log = console.log;

  fs = require('fs');

  path = require('path');

  uuid = require('node-uuid');

  vendor = function(name) {
    return fs.readFileSync(path.join(__dirname, '..', 'vendor', name)).toString();
  };

  jquery = vendor('jquery-1.8.0.min.js');

  sammy = vendor('sammy-0.7.1.min.js');

  socketjs = vendor('socket.io.min.js');

  uglify = require('uglify-js');

  socketio_key = '__session';

  jsdom = null;

  express_partials = null;

  coffee_css = null;

  coffeescript_helpers = "var __slice = Array.prototype.slice;\nvar __hasProp = Object.prototype.hasOwnProperty;\nvar __bind = function(fn, me){\n  return function(){ return fn.apply(me, arguments); };\n};\nvar __extends = function(child, parent) {\n  for (var key in parent) {\n    if (__hasProp.call(parent, key)) child[key] = parent[key];\n  }\n  function ctor() { this.constructor = child; }\n  ctor.prototype = parent.prototype;\n  child.prototype = new ctor;\n  child.__super__ = parent.prototype;\n  return child;\n};\nvar __indexOf = Array.prototype.indexOf || function(item) {\n  for (var i = 0, l = this.length; i < l; i++) {\n    if (this[i] === item) return i;\n  } return -1; };".replace(/\n/g, '');

  minify = function(js) {
    var ast;
    ast = uglify.parser.parse(js);
    ast = uglify.uglify.ast_mangle(ast);
    ast = uglify.uglify.ast_squeeze(ast);
    return uglify.uglify.gen_code(ast);
  };

  copy_data_to = function(recipient, sources) {
    var k, obj, v, _i, _len, _results;
    _results = [];
    for (_i = 0, _len = sources.length; _i < _len; _i++) {
      obj = sources[_i];
      _results.push((function() {
        var _results1;
        _results1 = [];
        for (k in obj) {
          v = obj[k];
          if (!recipient[k]) {
            _results1.push(recipient[k] = v);
          } else {
            _results1.push(void 0);
          }
        }
        return _results1;
      })());
    }
    return _results;
  };

  zappa_fs = {};

  native_name = function(p) {
    return p.replace(/\/\.zappa-[^\/]+/, '');
  };

  native_readFileSync = fs.readFileSync;

  native_readFile = fs.readFile;

  native_array = function() {
    var args;
    args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
    args[0] = native_name(args[0]);
    return args;
  };

  fs.readFileSync = function(p, encoding) {
    var _ref;
    return (_ref = zappa_fs[p]) != null ? _ref : native_readFileSync.apply(fs, native_array.apply(null, arguments));
  };

  fs.readFile = function(p, encoding, callback) {
    var view;
    view = zappa_fs[p];
    if (view) {
      if (typeof encoding === 'function' && !(callback != null)) {
        callback = encoding;
      }
      return callback(null, view);
    } else {
      return native_readFile.apply(fs, native_array.apply(null, arguments));
    }
  };

  native_existsSync = (_ref = fs.existsSync) != null ? _ref : path.existsSync;

  native_exists = (_ref1 = fs.exists) != null ? _ref1 : path.exists;

  path.existsSync = fs.existsSync = function(p) {
    return (zappa_fs[p] != null) || native_existsSync.apply(fs, native_array.apply(null, arguments));
  };

  path.exists = fs.exists = function(p, callback) {
    if (zappa_fs[p] != null) {
      return callback(true);
    } else {
      return native_exists.apply(fs, native_array.apply(null, arguments));
    }
  };

  express = require('express');

  socketio = require('socket.io');

  zappa.app = function(func, options) {
    var app, apply_helpers, client, client_bundled, context, helpers, io, name, partials, postrenders, real_root, request_socket, root, route, socket_session, verb, ws_handlers, zappa_used, _fn, _i, _j, _len, _len1, _ref2, _ref3, _ref4;
    if (options == null) {
      options = {};
    }
    context = {
      id: uuid(),
      zappa: zappa,
      express: express
    };
    real_root = path.dirname(module.parent.filename);
    root = path.join(real_root, ".zappa-" + context.id);
    ws_handlers = {};
    helpers = {};
    postrenders = {};
    partials = {};
    app = context.app = express();
    if (options.https != null) {
      context.server = require('https').createServer(options.https, app);
    } else {
      context.server = require('http').createServer(app);
    }
    if (options.disable_io) {
      io = null;
    } else {
      io = context.io = socketio.listen(context.server);
    }
    client = null;
    client_bundled = null;
    zappa_used = false;
    app.set('view engine', 'coffee');
    app.engine('coffee', coffeecup_adapter);
    app.set('views', path.join(root, '/views'));
    _ref2 = ['get', 'post', 'put', 'del', 'all'];
    _fn = function(verb) {
      return context[verb] = function() {
        var args, arity, k, v, _ref3, _results;
        args = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
        arity = args.length;
        if (arity > 1) {
          return route({
            verb: verb,
            path: args[0],
            middleware: args.slice(1, arity - 1),
            handler: args[arity - 1]
          });
        } else {
          _ref3 = arguments[0];
          _results = [];
          for (k in _ref3) {
            v = _ref3[k];
            _results.push(route({
              verb: verb,
              path: k,
              handler: v
            }));
          }
          return _results;
        }
      };
    };
    for (_i = 0, _len = _ref2.length; _i < _len; _i++) {
      verb = _ref2[_i];
      _fn(verb);
    }
    context.client = function(obj) {
      var js, k, v, _results;
      if (!zappa_used) {
        context.use('zappa');
      }
      _results = [];
      for (k in obj) {
        v = obj[k];
        js = ";zappa.run(" + v + ");";
        if (app.settings['minify']) {
          js = minify(js);
        }
        _results.push(route({
          verb: 'get',
          path: k,
          handler: js,
          contentType: 'js'
        }));
      }
      return _results;
    };
    context.coffee = function(obj) {
      var js, k, v, _results;
      _results = [];
      for (k in obj) {
        v = obj[k];
        js = ";" + coffeescript_helpers + "(" + v + ")();";
        if (app.settings['minify']) {
          js = minify(js);
        }
        _results.push(route({
          verb: 'get',
          path: k,
          handler: js,
          contentType: 'js'
        }));
      }
      return _results;
    };
    context.js = function(obj) {
      var js, k, v, _results;
      _results = [];
      for (k in obj) {
        v = obj[k];
        js = String(v);
        if (app.settings['minify']) {
          js = minify(js);
        }
        _results.push(route({
          verb: 'get',
          path: k,
          handler: js,
          contentType: 'js'
        }));
      }
      return _results;
    };
    context.css = function(obj) {
      var css, k, v, _results;
      _results = [];
      for (k in obj) {
        v = obj[k];
        if (typeof v === 'object') {
          if (coffee_css == null) {
            coffee_css = require('coffee-css');
          }
          css = coffee_css.compile(v);
        } else {
          css = String(v);
        }
        _results.push(route({
          verb: 'get',
          path: k,
          handler: css,
          contentType: 'css'
        }));
      }
      return _results;
    };
    if ((_ref3 = options.require_css) == null) {
      options.require_css = [];
    }
    if (typeof options.require_css === 'string') {
      options.require_css = [options.require_css];
    }
    _ref4 = options.require_css;
    for (_j = 0, _len1 = _ref4.length; _j < _len1; _j++) {
      name = _ref4[_j];
      context[name] = function(obj) {
        var css, k, v, _results;
        _results = [];
        for (k in obj) {
          v = obj[k];
          _results.push(css = require(name).render(v, {
            filename: k
          }, function(err, css) {
            if (err) {
              throw err;
            }
            return route({
              verb: 'get',
              path: k,
              handler: css,
              contentType: 'css'
            });
          }));
        }
        return _results;
      };
    }
    context.helper = function(obj) {
      var k, v, _results;
      _results = [];
      for (k in obj) {
        v = obj[k];
        _results.push(helpers[k] = v);
      }
      return _results;
    };
    context.postrender = function(obj) {
      var k, v, _results;
      jsdom = require('jsdom');
      _results = [];
      for (k in obj) {
        v = obj[k];
        _results.push(postrenders[k] = v);
      }
      return _results;
    };
    context.on = function(obj) {
      var k, v, _results;
      _results = [];
      for (k in obj) {
        v = obj[k];
        _results.push(ws_handlers[k] = v);
      }
      return _results;
    };
    context.view = function(obj) {
      var ext, k, p, v, _results;
      _results = [];
      for (k in obj) {
        v = obj[k];
        ext = path.extname(k);
        p = path.join(app.get('views'), k);
        zappa_fs[p] = v;
        if (!ext) {
          ext = '.' + app.get('view engine');
          _results.push(zappa_fs[p + ext] = v);
        } else {
          _results.push(void 0);
        }
      }
      return _results;
    };
    context.engine = function(obj) {
      var k, v, _results;
      _results = [];
      for (k in obj) {
        v = obj[k];
        _results.push(app.engine(k, v));
      }
      return _results;
    };
    context.set = function(obj) {
      var k, v, _results;
      _results = [];
      for (k in obj) {
        v = obj[k];
        _results.push(app.set(k, v));
      }
      return _results;
    };
    context.enable = function() {
      var i, _k, _len2, _results;
      _results = [];
      for (_k = 0, _len2 = arguments.length; _k < _len2; _k++) {
        i = arguments[_k];
        _results.push(app.enable(i));
      }
      return _results;
    };
    context.disable = function() {
      var i, _k, _len2, _results;
      _results = [];
      for (_k = 0, _len2 = arguments.length; _k < _len2; _k++) {
        i = arguments[_k];
        _results.push(app.disable(i));
      }
      return _results;
    };
    context.use = function() {
      var a, k, use, v, zappa_middleware, _k, _len2, _results;
      zappa_middleware = {
        "static": function(p) {
          if (p == null) {
            p = path.join(real_root, '/public');
          }
          return express["static"](p);
        },
        zappa: function() {
          zappa_used = true;
          return function(req, res, next) {
            var send;
            send = function(code) {
              res.contentType('js');
              return res.send(code);
            };
            if (req.method.toUpperCase() !== 'GET') {
              return next();
            } else {
              switch (req.url) {
                case '/zappa/Zappa.js':
                  return send(client_bundled);
                case '/zappa/zappa.js':
                  return send(client);
                case '/zappa/jquery.js':
                  return send(jquery);
                case '/zappa/sammy.js':
                  return send(sammy);
                default:
                  return next();
              }
            }
          };
        },
        partials: function(maps) {
          var k, v;
          if (maps == null) {
            maps = {};
          }
          if (express_partials == null) {
            express_partials = require('zappajs-partials');
          }
          partials = express_partials();
          partials.register('coffee', coffeecup_adapter.render);
          for (k in maps) {
            v = maps[k];
            partials.register(k, v);
          }
          return partials;
        },
        session: function(options) {
          context.session_store = options.store;
          return express.session(options);
        }
      };
      use = function(name, arg) {
        if (arg == null) {
          arg = null;
        }
        if (zappa_middleware[name]) {
          return app.use(zappa_middleware[name](arg));
        } else if (typeof express[name] === 'function') {
          return app.use(express[name](arg));
        }
      };
      _results = [];
      for (_k = 0, _len2 = arguments.length; _k < _len2; _k++) {
        a = arguments[_k];
        switch (typeof a) {
          case 'function':
            _results.push(app.use(a));
            break;
          case 'string':
            _results.push(use(a));
            break;
          case 'object':
            if ((a.stack != null) || (a.route != null)) {
              _results.push(app.use(a));
            } else {
              _results.push((function() {
                var _results1;
                _results1 = [];
                for (k in a) {
                  v = a[k];
                  _results1.push(use(k, v));
                }
                return _results1;
              })());
            }
            break;
          default:
            _results.push(void 0);
        }
      }
      return _results;
    };
    context.configure = function(p) {
      var k, v, _results;
      if (typeof p === 'function') {
        return app.configure(p);
      } else {
        _results = [];
        for (k in p) {
          v = p[k];
          _results.push(app.configure(k, v));
        }
        return _results;
      }
    };
    context.settings = app.settings;
    context.locals = app.locals;
    context.shared = function(obj) {
      var js, k, v, _results;
      if (!zappa_used) {
        context.use('zappa');
      }
      _results = [];
      for (k in obj) {
        v = obj[k];
        js = ";zappa.run(" + v + ");";
        if (app.settings['minify']) {
          js = minify(js);
        }
        route({
          verb: 'get',
          path: k,
          handler: js,
          contentType: 'js'
        });
        _results.push(v.apply(context));
      }
      return _results;
    };
    context.include = function(p) {
      var sub;
      sub = typeof p === 'string' ? require(path.join(real_root, p)) : p;
      return sub.include.apply(context);
    };
    apply_helpers = function(ctx) {
      var helper, _fn1;
      _fn1 = function(name, helper) {
        if (typeof helper === 'function') {
          return ctx[name] = function() {
            return helper.apply(ctx, arguments);
          };
        } else {
          return ctx[name] = helper;
        }
      };
      for (name in helpers) {
        helper = helpers[name];
        _fn1(name, helper);
      }
      return ctx;
    };
    request_socket = function(req) {
      var socket_id, _ref5, _ref6, _ref7;
      socket_id = (_ref5 = req.session) != null ? (_ref6 = _ref5.__socket) != null ? (_ref7 = _ref6['__local']) != null ? _ref7.id : void 0 : void 0 : void 0;
      return socket_id && (io != null ? io.sockets.socket(socket_id, true) : void 0);
    };
    socket_session = function(socket, cb) {
      return socket.get(socketio_key, function(err, data) {
        if (err) {
          return cb(err);
        }
        data = JSON.parse(data);
        if (data.id != null) {
          return context.session_store.get(data.id, cb);
        } else {
          return cb(err);
        }
      });
    };
    context.param = function(obj) {
      var build, k, v, _results;
      build = function(callback) {
        return function(req, res, next, p) {
          var ctx;
          ctx = {
            app: app,
            settings: app.settings,
            locals: res.locals,
            request: req,
            req: req,
            query: req.query,
            params: req.params,
            body: req.body,
            session: req.session,
            response: res,
            res: res,
            next: next,
            param: p
          };
          apply_helpers(ctx);
          return callback.apply(ctx);
        };
      };
      _results = [];
      for (k in obj) {
        v = obj[k];
        _results.push(this.app.param(k, build(v)));
      }
      return _results;
    };
    route = function(r) {
      var _ref5;
      if ((_ref5 = r.middleware) == null) {
        r.middleware = [];
      }
      r.middleware = r.middleware.map(function(f) {
        return function(req, res, next) {
          var ctx;
          ctx = {
            app: app,
            settings: app.settings,
            locals: res.locals,
            request: req,
            req: req,
            query: req.query,
            params: req.params,
            body: req.body,
            session: req.session,
            response: res,
            res: res,
            next: next
          };
          apply_helpers(ctx);
          if (app.settings['databag']) {
            ctx.data = {};
            copy_data_to(ctx.data, [req.query, req.params, req.body]);
          }
          return f.apply(ctx);
        };
      });
      if (typeof r.handler === 'string') {
        return app[r.verb].apply(app, [r.path].concat(__slice.call(r.middleware), [function(req, res) {
          if (r.contentType != null) {
            res.contentType(r.contentType);
          }
          return res.send(r.handler);
        }]));
      } else {
        return app[r.verb].apply(app, [r.path].concat(__slice.call(r.middleware), [function(req, res, next) {
          var ctx, render, result;
          ctx = {
            app: app,
            settings: app.settings,
            locals: res.locals,
            request: req,
            req: req,
            query: req.query,
            params: req.params,
            body: req.body,
            session: req.session,
            response: res,
            res: res,
            next: next,
            send: function() {
              return res.send.apply(res, arguments);
            },
            json: function() {
              return res.json.apply(res, arguments);
            },
            redirect: function() {
              return res.redirect.apply(res, arguments);
            },
            render: function() {
              var k, v, _ref6, _results;
              if (typeof arguments[0] !== 'object') {
                return render.apply(this, arguments);
              } else {
                _ref6 = arguments[0];
                _results = [];
                for (k in _ref6) {
                  v = _ref6[k];
                  _results.push(render.apply(this, [k, v]));
                }
                return _results;
              }
            },
            emit: function() {
              var k, socket, v, _ref6, _results;
              socket = request_socket(req);
              if (socket != null) {
                if (typeof arguments[0] !== 'object') {
                  return socket.emit.apply(socket, arguments);
                } else {
                  _ref6 = arguments[0];
                  _results = [];
                  for (k in _ref6) {
                    v = _ref6[k];
                    _results.push(socket.emit.apply(socket, [k, v]));
                  }
                  return _results;
                }
              }
            }
          };
          render = function(name, opts, fn) {
            var postrender, report;
            if (opts == null) {
              opts = {};
            }
            report = fn != null ? fn : function(err, html) {
              if (err) {
                return next(err);
              } else {
                return res.send(html);
              }
            };
            if (typeof opts === 'function') {
              fn = opts;
              opts = {};
            }
            if (app.settings['databag']) {
              opts.params = data;
            }
            if (!(opts.postrender != null)) {
              postrender = report;
            } else {
              postrender = function(err, str) {
                if (err) {
                  return report(err);
                }
                return jsdom.env({
                  html: str,
                  src: [jquery],
                  done: function(err, window) {
                    var doctype, html, rendered;
                    if (err) {
                      return report(err);
                    }
                    ctx.window = window;
                    rendered = postrenders[opts.postrender].apply(ctx, [window.$]);
                    doctype = (window.document.doctype || '') + "\n";
                    html = doctype + window.document.documentElement.outerHTML;
                    return report(null, html);
                  }
                });
              };
            }
            return res.render.call(res, name, opts, postrender);
          };
          apply_helpers(ctx);
          if (app.settings['databag']) {
            ctx.data = {};
            copy_data_to(ctx.data, [req.query, req.params, req.body]);
          }
          result = r.handler.apply(ctx);
          if (r.contentType != null) {
            res.contentType(r.contentType);
          }
          if (typeof result === 'string') {
            return res.send(result);
          } else {
            return result;
          }
        }]));
      }
    };
    if (io != null) {
      io.sockets.on('connection', function(socket) {
        var build_ctx, c, ctx, h, _results;
        c = {};
        build_ctx = function() {
          var ctx;
          ctx = {
            app: app,
            io: io,
            settings: app.settings,
            locals: app.locals,
            socket: socket,
            id: socket.id,
            client: c,
            join: function(room) {
              return socket.join(room);
            },
            leave: function(room) {
              return socket.leave(room);
            },
            emit: function() {
              var k, v, _ref5, _results;
              if (typeof arguments[0] !== 'object') {
                return socket.emit.apply(socket, arguments);
              } else {
                _ref5 = arguments[0];
                _results = [];
                for (k in _ref5) {
                  v = _ref5[k];
                  _results.push(socket.emit.apply(socket, [k, v]));
                }
                return _results;
              }
            },
            broadcast: function() {
              var broadcast, k, v, _ref5, _results;
              broadcast = socket.broadcast;
              if (typeof arguments[0] !== 'object') {
                return broadcast.emit.apply(broadcast, arguments);
              } else {
                _ref5 = arguments[0];
                _results = [];
                for (k in _ref5) {
                  v = _ref5[k];
                  _results.push(broadcast.emit.apply(broadcast, [k, v]));
                }
                return _results;
              }
            },
            broadcast_to: function() {
              var args, k, room, v, _ref5, _results;
              room = arguments[0], args = 2 <= arguments.length ? __slice.call(arguments, 1) : [];
              room = io.sockets["in"](room);
              if (typeof args[0] !== 'object') {
                return room.emit.apply(room, args);
              } else {
                _ref5 = args[0];
                _results = [];
                for (k in _ref5) {
                  v = _ref5[k];
                  _results.push(room.emit.apply(room, [k, v]));
                }
                return _results;
              }
            },
            session: function() {
              return socket_session.apply(null, [socket].concat(__slice.call(arguments)));
            }
          };
          apply_helpers(ctx);
          return ctx;
        };
        ctx = build_ctx();
        if (ws_handlers.connection != null) {
          ws_handlers.connection.apply(ctx);
        }
        socket.on('disconnect', function() {
          ctx = build_ctx();
          if (ws_handlers.disconnect != null) {
            return ws_handlers.disconnect.apply(ctx);
          }
        });
        _results = [];
        for (name in ws_handlers) {
          h = ws_handlers[name];
          _results.push((function(name, h) {
            if (name !== 'connection' && name !== 'disconnect') {
              return socket.on(name, function(data, ack) {
                ctx = build_ctx();
                ctx.data = data;
                ctx.ack = ack;
                return h.apply(ctx);
              });
            }
          })(name, h));
        }
        return _results;
      });
    }
    func.apply(context);
    client = require('./client').build(zappa.version, app.settings);
    client = ";" + coffeescript_helpers + "(" + client + ")();";
    if (app.settings['minify']) {
      client = minify(client);
    }
    client_bundled = io != null ? jquery + socketjs + sammy + client : jquery + sammy + client;
    if (app.settings['default layout']) {
      context.view({
        layout: function() {
          doctype(5);
          return html(function() {
            head(function() {
              var s, _k, _l, _len2, _len3, _ref5, _ref6;
              if (this.title) {
                title(this.title);
              }
              if (this.scripts) {
                _ref5 = this.scripts;
                for (_k = 0, _len2 = _ref5.length; _k < _len2; _k++) {
                  s = _ref5[_k];
                  script({
                    src: s + '.js'
                  });
                }
              }
              if (this.script) {
                script({
                  src: this.script + '.js'
                });
              }
              if (this.stylesheets) {
                _ref6 = this.stylesheets;
                for (_l = 0, _len3 = _ref6.length; _l < _len3; _l++) {
                  s = _ref6[_l];
                  link({
                    rel: 'stylesheet',
                    href: s + '.css'
                  });
                }
              }
              if (this.stylesheet) {
                link({
                  rel: 'stylesheet',
                  href: this.stylesheet + '.css'
                });
              }
              if (this.style) {
                return style(this.style);
              }
            });
            return body(this.body);
          });
        }
      });
    }
    if (io != null) {
      context.get('/zappa/socket/:channel_name/:socket_id', function() {
        var channel_name, io_client, io_data, key, socket_id, _base, _ref5;
        if (this.session != null) {
          channel_name = this.params.channel_name;
          socket_id = this.params.socket_id;
          if ((_ref5 = (_base = this.session).__socket) == null) {
            _base.__socket = {};
          }
          if (this.session.__socket[channel_name] != null) {
            return this.send({
              error: 'Channel already assigned',
              channel_name: channel_name
            });
          } else {
            key = uuid();
            this.session.__socket[channel_name] = {
              id: socket_id,
              key: key
            };
            io_client = io.sockets.store.client(socket_id);
            io_data = JSON.stringify({
              id: this.req.sessionID,
              key: key
            });
            io_client.set(socketio_key, io_data);
            return this.send({
              channel_name: channel_name,
              key: key
            });
          }
        } else {
          return this.send({
            error: 'No session'
          });
        }
      });
    }
    return context;
  };

  zappa.run = function() {
    var a, app, host, k, options, port, root_function, v, zapp, _i, _len, _ref2;
    host = null;
    port = 3000;
    root_function = null;
    options = {
      disable_io: false,
      require_css: ['stylus']
    };
    for (_i = 0, _len = arguments.length; _i < _len; _i++) {
      a = arguments[_i];
      switch (typeof a) {
        case 'string':
          if (isNaN(Number(a))) {
            host = a;
          } else {
            port = Number(a);
          }
          break;
        case 'number':
          port = a;
          break;
        case 'function':
          root_function = a;
          break;
        case 'object':
          for (k in a) {
            v = a[k];
            switch (k) {
              case 'host':
                host = v;
                break;
              case 'port':
                port = v;
                break;
              case 'css':
                options.require_css = v;
                break;
              case 'disable_io':
                options.disable_io = v;
                break;
              case 'https':
                options.https = v;
            }
          }
      }
    }
    zapp = zappa.app(root_function, options);
    app = zapp.app;
    if (host) {
      zapp.server.listen(port, host);
    } else {
      zapp.server.listen(port);
    }
    log('Express server listening on port %d in %s mode', (_ref2 = zapp.server.address()) != null ? _ref2.port : void 0, app.settings.env);
    log("Zappa " + zappa.version + " \"" + codename + "\" orchestrating the show");
    return zapp;
  };

  zappa.adapter = function(engine, options) {
    var compile, render, renderFile, _ref2;
    if (options == null) {
      options = {};
    }
    if ((_ref2 = options.blacklist) == null) {
      options.blacklist = [];
    }
    if (typeof engine === 'string') {
      engine = require(engine);
    }
    compile = function(template, data) {
      template = engine.compile(template, data);
      return function(data) {
        var k, v, _ref3;
        _ref3 = data.params;
        for (k in _ref3) {
          v = _ref3[k];
          if (typeof data[k] === 'undefined' && __indexOf.call(options.blacklist, k) < 0) {
            data[k] = v;
          }
        }
        return template(data);
      };
    };
    render = function(template, data) {
      template = compile(template, data);
      return template(data);
    };
    renderFile = function(name, data, fn) {
      var template;
      try {
        template = fs.readFileSync(name, 'utf8');
        template = compile(template, data);
      } catch (err) {
        return fn(err);
      }
      return fn(null, template(data));
    };
    renderFile.compile = compile;
    renderFile.render = render;
    return renderFile;
  };

  coffeecup_adapter = zappa.adapter('coffeecup', {
    blacklist: ['format', 'autoescape', 'locals', 'hardcode', 'cache']
  });

  module.exports = zappa.run;

  module.exports.run = zappa.run;

  module.exports.app = zappa.app;

  module.exports.adapter = zappa.adapter;

  module.exports.version = zappa.version;

}).call(this);
